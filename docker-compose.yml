services:
  postgres:
    image: postgres:16-alpine
    container_name: easi-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-easi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-easi}
      POSTGRES_DB: ${POSTGRES_DB:-easi}
    volumes:
      - easi_postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U easi"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - easi-network

  migrate:
    build:
      context: ./backend
      dockerfile: Dockerfile.migrate
    container_name: easi-migrate
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_ADMIN_USER: ${DB_ADMIN_USER:-easi}
      DB_ADMIN_PASSWORD: ${DB_ADMIN_PASSWORD:-easi}
      DB_NAME: ${POSTGRES_DB:-easi}
      MIGRATIONS_PATH: /app/migrations
    volumes:
      - ./backend/migrations:/app/migrations:ro
    networks:
      - easi-network
    depends_on:
      postgres:
        condition: service_healthy

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: easi-backend
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-easi_app}
      DB_PASSWORD: ${DB_PASSWORD:-easi}
      DB_NAME: ${POSTGRES_DB:-easi}
      PORT: 8080
    ports:
      - "8080:8080"
    networks:
      - easi-network
    depends_on:
      migrate:
        condition: service_completed_successfully

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: easi-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@easi.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    volumes:
      - easi_pgadmin_data:/var/lib/pgadmin
    ports:
      - "5050:80"
    networks:
      - easi-network
    depends_on:
      - postgres

volumes:
  easi_postgres_data:
  easi_pgadmin_data:

networks:
  easi-network:
    driver: bridge
